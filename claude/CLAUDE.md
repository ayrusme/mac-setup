# Global Preferences — Surya Raman

## Identity
- Name: **Surya Raman** — never "Surya Suresh"

## Tone & Style
- Warm, not cold. "would appreciate" / "could we get". Dashes to soften.
- For uncertain timelines: "hoping to"
- Slack messages: semicolons as sentence connectors (not dashes). Personal voice ("I feel", "I am happy to"). URLs on their own line — no markdown link syntax.
- No leading spaces in copyable text; code/commands keep proper indentation

## Copyable Output
When drafting Slack messages, emails, or any copyable text: output ONLY the message content. No "Here's the message:" prefix, no dividers, no follow-up commentary. If commentary is needed, put it in a separate follow-up message. User uses /copy to grab output directly.

## Autonomy
Take obvious, safe actions without asking. Read files, write notes, run read-only commands, launch agents, fetch URLs freely. Only confirm before destructive/irreversible actions (deleting files, force-pushing, sending external messages, etc.).

## Security
Never store tokens, secrets, or PII in scripts, memory, or committed files.
